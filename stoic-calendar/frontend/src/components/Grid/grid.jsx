import React from 'react'
import './Grid.css'

//generate a grid of squares empty and nonempty depending on duration lived
export default function Grid ({}) {
    let rows = 64;
    let columns = 52;
    let weekCounter = 0;

    //create a 
    for (let i = 0; i < rows; i++) {

        for (let j = 0; j < columns; j++) {
            
            // //generate a filled/empty square depending on if weekCounter has met durationLived yet
            // if (weekCounter <= durationLived) {

            // }
            // else {

            // }
            
        }
    }
    
    //loop for 18 y axes 

    //drop down one square

    //place a square 52 times
}